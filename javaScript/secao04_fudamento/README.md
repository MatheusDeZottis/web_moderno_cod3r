Neste REDME está diponivel as imformações sobre este capitulo resumida.

Esses são os principais pontos abordados nas 44 aulas do capítulo de fudamentos.

### 1. Algoritmos
* Conceito de algoritmos: sequências de passos para resolver problemas.
* Importância de algoritmos na programação.

### 2. Estruturas de Dados
* Introdução às estruturas de dados.
* Tipos de estruturas de dados comuns (arrays, listas, árvores, etc.).

### 3. Organização Básica do JavaScript
* Estrutura básica de um código JavaScript.
* Boas práticas de organização de código.

### 4. Comentários em um Código
* Importância dos comentários para documentação e legibilidade.
* Sintaxe de comentários em JavaScript (`//` para linha única, `/* */` para múltiplas linhas).

### 5. Variáveis: `var`, `let` e `const`
* Diferenças entre `var`, `let` e `const`.
* Escopo de variáveis (`var` tem escopo de função, `let` e `const` têm escopo de bloco).
* Imutabilidade de `const`.

### 6. Tipagem Fraca
* Conceito de tipagem fraca em JavaScript.
* Implicações da tipagem fraca para a flexibilidade e potencial para erros.

### 7. Tipo `Number` em JavaScript
* Operações com números.
* Tipos de números (inteiros, flutuantes).
* Funções úteis para números (`parseInt`, `parseFloat`, etc.).

### 8. Objeto `Math`
* Funções matemáticas disponíveis no objeto `Math`.
* Exemplos de uso: `Math.random()`, `Math.round()`, `Math.floor()`, etc.

### 9. Arrays
* Criação e manipulação de arrays.
* Métodos úteis para arrays (`push`, `pop`, `shift`, `unshift`, `map`, `filter`, etc.).

### 10. Objetos em JavaScript
* Definição e uso de objetos.
* Propriedades e métodos de objetos.
* Acesso e modificação de propriedades.

### 11. `null` & `undefined`
* Diferença entre `null` e `undefined`.
* Uso apropriado de cada um.

### 12. Funções
* Declaração e invocação de funções.
* Funções anônimas e funções de flecha (arrow functions).
* Parâmetros e retorno de funções.

### 13. Loops
* Tipos de loops em JavaScript (`for`, `while`, `do...while`, `for...in`, `for...of`).
* Uso de loops para iterar sobre arrays e objetos.

### 14. Operadores
* Operadores aritméticos, de comparação, lógicos e de atribuição.
* Operadores ternários e de coalescência nula (`??`).

### 15. Tratamento de Erro
* Uso do bloco `try...catch`.
* Lançamento de erros com `throw`.
* Boa prática de gerenciamento de erros para evitar que a aplicação quebre.

